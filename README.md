# simulator2d
Simple 2D simulator project for learning about Rust

## Clone and Compile
```
git clone https://github.com/daanario/simulator2d && cd simulator2d
```
```
cargo build --release
```

First compilation may take a while, but subsequent builds should be faster due to incremental compilation.

Screenshot: [example.png]
